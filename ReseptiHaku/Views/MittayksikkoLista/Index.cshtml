@model IEnumerable<ReseptiHaku.Models.MittayksikkoLista>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Mittayksikko)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.MittayksikkoSelite)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Mittayksikko)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.MittayksikkoSelite)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.MittayksikkoID }) |
            @Html.ActionLink("Details", "Details", new { id=item.MittayksikkoID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.MittayksikkoID })
        </td>
    </tr>
}

</table>
