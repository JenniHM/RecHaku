@model IEnumerable<ReseptiHaku.Models.RaakaAineet>

@{
    ViewBag.Title = "Index";
}

<h2>Raaka-aineet - [dbo].[RaakaAineet]</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.RaakaAine)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Kategoriat.Kategoria)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.RaakaAine)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Kategoriat.Kategoria)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.RaakaAineID }) |
            @Html.ActionLink("Details", "Details", new { id=item.RaakaAineID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.RaakaAineID })
        </td>
    </tr>
}

</table>
